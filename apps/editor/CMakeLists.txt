roboslop_add_module_library(editor_model MODULES src/model.cppm)
target_link_libraries(editor_model PUBLIC roboslop)
add_executable(editor src/main.cpp)
target_link_libraries(editor PRIVATE roboslop editor_model)
roboslop_apply_language_defaults(editor)
roboslop_set_warnings(editor)
roboslop_apply_sanitizers(editor)
if(ROBOSLOP_BUILD_TESTS)
    find_package(Catch2 3 CONFIG REQUIRED)
    add_executable(editor_tests tests/model_test.cpp)
    target_link_libraries(editor_tests PRIVATE editor_model Catch2::Catch2WithMain)
    roboslop_apply_language_defaults(editor_tests)
    roboslop_set_warnings(editor_tests)
    roboslop_apply_sanitizers(editor_tests)
    include(Catch)
    catch_discover_tests(editor_tests)
    add_executable(editor_runtime_smoke tests/runtime_smoke.cpp)
    target_link_libraries(editor_runtime_smoke PRIVATE editor_model roboslop)
    roboslop_apply_language_defaults(editor_runtime_smoke)
    roboslop_set_warnings(editor_runtime_smoke)
    roboslop_apply_sanitizers(editor_runtime_smoke)
endif()
